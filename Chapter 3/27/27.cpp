/*
  a. Illegal because the dimension of the array must be a constant expression.
  b. Legal.
  c. Illegal for the same reason as (a).
  d. Illegal because it does not allow space for the null character, '\0'.
 */
