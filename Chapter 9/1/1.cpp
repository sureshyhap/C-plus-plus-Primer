/*
(a) list or forward_list because inserting into the middle will be necessary to keep the data sorted alphabetically as they are entered
(b) deque because it has fast insertion and removal at both ends
(c) vector because the values can all be inserted at the end and then sorted once
*/
