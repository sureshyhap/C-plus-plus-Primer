/*
  a. Illegal. int i, const pointer to int cp needs to be initialized
  b. Illegal pointer to int pi, const pointer p2 needs to be initialized 
  c. Illegal const int ic needs to be initialized, reference to const r
  d. Illegal const pointer to const int p3 needs to be initialized
  e. Legal pointer to const int p doesn't need to be initialized
 */
