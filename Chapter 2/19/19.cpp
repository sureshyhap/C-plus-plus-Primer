/*
  Pointers are objects in their own right. References are not; they are
  synonyms for another object. Pointers can be uninitialized while
  references must be initialized to an object at its creation. Pointers
  can change what they point to while references cannot change what they
  refer to.
 */
