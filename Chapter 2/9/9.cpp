/*
  a. Illegal, can't initialize and pass to cin.
  -> int input_value; std::cin >> input_value;
  b. Illegal, can't use curly brace initialization
  if narrowing conversion if required. -> int i = 3.14;
  c. Legal, assigns 9999.99 to wage and then wage to salary.
  d. Legal, assigns 3 to i.
 */
