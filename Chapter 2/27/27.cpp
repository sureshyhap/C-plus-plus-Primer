/*
  a. i assignment is legal but r reference assignment is not.
  r must be bound to a variable, not a literal.
  b. Legal because a const pointer can be initialized
  c. Legal because a reference to a const can refer to a literal
  d. Legal
  e. Legal
  f. Illegal
  g. Legal because a const reference can refer to a nonconst int
 */
