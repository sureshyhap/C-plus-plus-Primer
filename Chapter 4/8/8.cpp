/*
  The second operand to && only evaluates if the left operand evaluated to true.
  The second operand to || only evaluates if the left operand evaluated to false.
  ? Unsure what they mean for equality operator order. ?
 */
