/*
  a. The character 'a' is promoted to int and then back to char.
  b. ival is promoted to double then the right side becomes double and is changed to float
  c. Changed to double.
  d. Sums ultimately promote to double but then are changed to char for the assignment
 */
