/*
  They should be const if the reference is not meant to change the object.
  It allows changing something that probably shouldn't be changed and limits it use. You cannot pass in const objects or literals.
 */
