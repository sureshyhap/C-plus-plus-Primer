/*
  a. The Sales_data constructor with a C++ string argument will implicitly be called (if it's not labeled as explicit) creating a temporary Sales_data object that will be used.
  b. The temporary Sales data object cannot be returned because it won't last.
  c. Same as aboe.


 */
