/*
  a. while the end of a string has not been encountered, keep going
  b. The loop keeps going if status continually gets set to true. A problem is that the if statement uses status but status is local to the while loop.
 */
